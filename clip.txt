clip 属性剪裁绝对定位元素
1、这个属性用于定义一个剪裁矩形。
2、对于一个绝对定义元素，在这个矩形内的内容才可见。
3、出了这个剪裁区域的内容会根据 overflow 的值来处理。
4、剪裁区域可能比元素的内容区大，也可能比内容区小

clip:rect(0px,156px,0px,72px);
四个方向值分别为 top right bottom left
---分别指最终剪裁可见区域的上边，右边，下边与左边
如果left >= right或者bottom <= top，
则元素会被完全裁掉而不可见，即“隐藏”。